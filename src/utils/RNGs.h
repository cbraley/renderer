#ifndef RNGS_H
#define RNGS_H

/*
namespace RNG{
    void initializeRNGs(bool useTimeSeed = true);
    float randomFloat(const float minIncl, const float maxExcl);
    int randomInt(const int minIncl, const int maxExcl);
    double randomDouble(const double minIncl, const double maxExcl);
}
*/

#endif //RNGS_H
